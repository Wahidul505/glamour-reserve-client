import Navbar from "@/components/ui/Navbar/Navbar";
import React from "react";

const MainPage = () => {
  return (
    <div>
      <Navbar sidebar={true} />
    </div>
  );
};

export default MainPage;
